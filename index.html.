<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Revell Roofing – Roofing specialists in Essex. Roof repairs, new roofs, leadwork, inspections and gutter cleaning. Fast reply, responsibly priced.">
    <title>Revell Roofing | Roofing Specialists Essex</title>

    <style>
        :root {
            --red: #e31e24;
            --black: #000000;
            --white: #ffffff;
            --green: #25D366;
        }

        * {
            box-sizing: border-box;
        }

        body {
            font-family: Arial, Helvetica, sans-serif;
            background-color: var(--black);
            color: var(--white);
            margin: 0;
            padding: 0;
            text-align: center;
        }

        .container {
            padding: 20px;
            max-width: 600px;
            margin: auto;
        }

        .logo-area {
            border-bottom: 2px solid var(--white);
            padding-bottom: 20px;
            margin-bottom: 20px;
        }

        h1 {
            color: var(--red);
            font-size: clamp(2.2rem, 6vw, 3rem);
            margin: 0;
            line-height: 0.95;
            text-transform: uppercase;
            font-weight: 900;
        }

        .subtitle {
            font-size: 1.2rem;
            letter-spacing: 2px;
            margin-top: 12px;
            border-top: 2px solid var(--white);
            display: inline-block;
            padding-top: 10px;
        }

        .services {
            list-style: none;
            padding: 0;
            font-size: 1.1rem;
            line-height: 1.8;
            margin: 30px 0;
        }

        .services li {
            border-bottom: 1px solid #333;
            padding: 10px 0;
            text-transform: uppercase;
            font-weight: 600;
        }

        .contact-btn {
            display: block;
            background: var(--white);
            color: var(--black);
            text-decoration: none;
            padding: 16px;
            border-radius: 8px;
            font-weight: bold;
            margin: 12px 0;
            font-size: 1.15rem;
            transition: transform 0.15s ease, opacity 0.15s ease;
        }

        .contact-btn:hover {
            transform: scale(0.98);
            opacity: 0.9;
        }

        .whatsapp {
            background: var(--green);
            color: var(--white);
        }

        .footer-info {
            font-size: 0.85rem;
            color: #888;
            margin-top: 40px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        a {
            color: inherit;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="logo-area">
            <h1>REVELL<br>ROOFING</h1>
            <div class="subtitle">ROOFING SPECIALISTS</div>
        </div>

        <ul class="services">
            <li>Roof Repairs</li>
            <li>New Roofs</li>
            <li>Lead Work</li>
            <li>Roof Inspections</li>
            <li>Gutter Cleaning</li>
            <li>Responsibly Priced</li>
        </ul>

        <a href="tel:07881681124" class="contact-btn">📞 CALL: 07881 681124</a>
        <a href="mailto:revellroofing2020@gmail.com" class="contact-btn">✉️ EMAIL US</a>
        <a href="https://wa.me/447881681124" class="contact-btn whatsapp">💬 WHATSAPP US</a>

        <div class="footer-info">
            ESSEX BASED (CLACTON-ON-SEA) • NO TIME WASTERS • FAST REPLY
        </div>
    </div>
</body>
</html>
